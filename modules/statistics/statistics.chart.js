
define("statistics/chart", [
    "leaflet",
    "core/dcins",
    "library/echarts"
], function (L, ec) {
    L.DCI.Chart = L.Class.extend({

        initialize: function () {
        }
    });
    return L.DCI.Chart;
});

